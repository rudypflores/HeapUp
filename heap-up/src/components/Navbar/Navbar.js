import React from 'react';
import './Navbar.css';

const Navbar = () => {
    return(
        <div id="navbar">
            <p id="title">Heap
            <span id="title-x">Up</span>
            <span id="title-y">!</span>
            </p>
        </div>
    );
};

export default Navbar;